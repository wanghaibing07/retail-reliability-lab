apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vendor/argocd-v3.5.2/install.yaml
  - resources/retail-repo.yaml

patches:
  - path: patches/argocd-cm.yaml
  - path: patches/repo-server-git-retry.yaml
