# 上游依赖锁定

本目录保存上游部署资产的版本说明、校验值和必要补丁，不直接存放未审查的临时下载物。

## 当前版本

- 项目：AWS Retail Store Sample App
- 目标版本：`v1.6.2`
- 部署清单：在下载并审查后保存到本目录
- 校验值：待 G1 基线阶段下载后计算并记录

建议流程：

1. 下载指定版本，而不是默认分支或 `latest`。
2. 查看变更、镜像引用、命名空间和 PVC 定义。
3. 计算文件 SHA-256 并记录。
4. 用静态校验和实验命名空间部署验证。
5. 任何本地修改都通过补丁或明确的 overlay 表达。
